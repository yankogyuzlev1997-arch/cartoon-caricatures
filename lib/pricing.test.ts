import {describe,expect,it} from 'vitest';import {calculateOrder,projectPriceCents} from './pricing';
describe('caricature pricing',()=>{it.each([['A4',1,6572],['A4',6,11572],['A3',3,13572],['A2',6,25572]] as const)('%s / %i лица = %i цента',(size,people,total)=>expect(projectPriceCents(size,people)).toBe(total));it('makes the cheapest of three same-size projects free',()=>expect(calculateOrder([{size:'A4',people:1},{size:'A4',people:2},{size:'A4',people:3}])).toEqual({subtotalCents:22716,discountPercent:29,discountCents:6572,totalCents:16144,savedCents:6572}));it('does not combine different sizes',()=>expect(calculateOrder([{size:'A4',people:1},{size:'A3',people:1},{size:'A2',people:1}]).discountCents).toBe(0));});
