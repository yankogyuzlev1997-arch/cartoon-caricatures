import {NextResponse} from 'next/server';import {orderSchema} from '@/lib/order-schema';import {calculateOrder} from '@/lib/pricing';
export async function POST(request:Request){const parsed=orderSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:'Моля, проверете въведените данни.',details:parsed.error.flatten()},{status:400});const totals=calculateOrder(parsed.data.projects.map(p=>({size:p.size,people:p.people})));return NextResponse.json({ok:true,totals,message:'Поръчката е валидирана на сървъра. Свържете Supabase, за да я запишете.'},{status:201})}
