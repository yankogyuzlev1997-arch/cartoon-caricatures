import {notFound} from 'next/navigation';import Link from 'next/link';import {money,patterns,products} from '@/lib/catalog';
export async function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <section className="section"><div className="container" style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(300px,1fr))',gap:60}}><div><div className="pattern" style={{height:600}}/><div style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:10,marginTop:10}}>{[1,2,3].map(x=><div className="pattern" style={{height:140}} key={x}/>)}</div></div><div><p className="eyebrow">Ръчна изработка в България</p><h1 style={{fontSize:58,marginBottom:10}}>{p.name}</h1><p style={{fontSize:22}}>{money(p.priceCents)}</p><p className="muted" style={{lineHeight:1.7}}>{p.description}</p><label className="field" style={{margin:'28px 0'}}>Изберете десен<select>{patterns.map(x=><option key={x}>{x}</option>)}</select></label><label className="field">Количество<input type="number" min="1" defaultValue="1"/></label>{p.laptop&&<div style={{background:'#eee3d5',padding:20,marginTop:22}}><b>Индивидуални мерки</b><p>Марка, модел, диагонал, ширина, височина и дебелина се потвърждават преди поръчка.</p><strong style={{color:'#7a1830'}}>Моля, проверете внимателно измерванията.</strong></div>}<button className="btn" style={{width:'100%',marginTop:25}}>Добави в кошницата</button><p className="muted">Добавете втори продукт и спестете 10%. Без фалшива спешност — изработваме с грижа.</p><hr style={{margin:'30px 0'}}/><p><b>Изработка:</b> ръчно, с внимание към всеки детайл.</p><p><b>Доставка:</b> срокът се потвърждава при поръчка.</p><Link href="/dostavka-i-vrashtane">Доставка и връщане →</Link></div></div></section>}
