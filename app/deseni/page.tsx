import {patterns} from '@/lib/catalog';export default function Page(){return <section className="section"><div className="container"><p className="eyebrow">Пет материи</p><h1 style={{fontSize:60}}>Десени с характер</h1><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:24}}>{patterns.map((p,i)=><article key={p}><div className="pattern" style={{height:i%2?380:460}}/><h2>{p}</h2><p className="muted">Име и фотография — редактирайте от администрацията.</p></article>)}</div></div></section>}
