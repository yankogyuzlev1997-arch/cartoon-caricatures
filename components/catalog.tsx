import Link from 'next/link';import {money,products} from '@/lib/catalog';
export function ProductGrid(){return <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(230px,1fr))',gap:24}}>{products.map((p,i)=><Link key={p.id} href={`/produkt/${p.slug}`} style={{color:'inherit',textDecoration:'none'}}><div className="pattern" style={{height:340,backgroundPosition:`${i*10}%`,position:'relative'}}><span style={{position:'absolute',left:14,bottom:14,background:'#fffdf8',padding:'7px 10px',fontSize:12}}>Ръчна изработка</span></div><p className="eyebrow" style={{marginBottom:8,marginTop:18}}>{p.eyebrow}</p><div style={{display:'flex',justifyContent:'space-between',gap:12}}><h3 style={{fontSize:24,margin:0}}>{p.name}</h3><span>{money(p.priceCents)}</span></div></Link>)}</div>}
